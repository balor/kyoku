// RT_MANIFEST (24) resource id 1 = CREATEPROCESS_MANIFEST_RESOURCE_ID.
// Numeric constants instead of winuser.h so llvm-rc (used by
// embed-resource for cross and GNU-ABI builds) compiles this without
// the Windows SDK resource headers.
#pragma code_page(65001)
1 24 "kyoku.manifest"